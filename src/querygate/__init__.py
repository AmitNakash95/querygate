"""QueryGate — an agent-safe database access gateway.

Exposes read-only, policy-enforced structured queries over MCP and REST
without ever accepting raw SQL from a caller.
"""

__version__ = "0.1.0"
