{
  "name": "@querygate/query-builder",
  "version": "0.1.0",
  "private": true,
  "description": "Typed, fluent builder for QueryGate's StructuredQuery AST (TODO.md item 51, phase 2). In-tree only — not yet published to a registry (blocked on item 30 phase 2's package-registry decision).",
  "type": "commonjs",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/test/*.test.js",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
