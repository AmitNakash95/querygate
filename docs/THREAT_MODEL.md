# QueryGate threat model

**Status:** first-party security model for QueryGate 0.1.x
**Last reviewed:** 2026-08-02
**Scope:** the REST and MCP request paths, authentication, policy/schema
validation, query compilation and execution, Redis concurrency coordination,
configuration reload, the config-governance version store and semantic
access diff, secret
resolution, versioned semantic-catalog provenance/schema fingerprints,
manual-only draft quarantine, automatic schema refresh, policy-first catalog
retrieval, the catalog-governance review/publish/rollback/export/import/
deletion workflow, and audit/log outputs in this repository.

This document explains what QueryGate is designed to defend, which controls
exist in code, and which risks remain with the operator. It is not an external
penetration test, compliance certification, or guarantee that a deployment is
secure regardless of its configuration.

## 1. Security objective

QueryGate permits an authenticated AI agent or application to perform bounded,
read-only, structured database queries without receiving a raw-SQL capability.
The gateway must preserve these properties even when the caller is malicious,
prompt-injected, confused about its permissions, or repeatedly sends unusual
but schema-valid AST combinations:

1. A caller can only discover and query connections, tables, and columns
   allowed by the policy resolved for that principal.
2. Predicate values remain bound data and cannot become executable SQL.
3. Tenant/row restrictions cannot be omitted by the caller.
4. Responses, public errors, schemas, and persisted audit events do not expose
   credentials or unintended database contents.
5. Query concurrency, duration, row count, batch size, and output bytes are
   bounded according to policy.
6. Security-relevant decisions remain attributable through audit events and
   correlation identifiers.

## 2. System and trust boundaries

```text
Untrusted agent/client
        |
        | HTTPS: REST or MCP
        v
Reverse proxy / load balancer  [operator boundary]
        |
        v
QueryGate authentication      -----> JWKS identity provider
        |
        v
Principal-aware policy + schema validation
        |
        v
SQLAlchemy compiler (SELECT only, bound values)
        |
        +-----> Redis concurrency coordinator
        |
        v
Customer database (read-only account recommended)
        |
        +-----> structured result caps
        +-----> stdout logs / persisted audit JSONL

Config load/reload (operator-triggered via /admin/reload-config, or
proactively via config_reload.CredentialLeaseMonitor — item 135)
        |
        +-----> secrets/resolvers.py (env, optionally Vault KV v2;
                LeasedSecretResolver reports a lease expiry when the
                backend has one)

Admin caller (admin:config:read / admin:config:write)
        |
        v
admin/service.py — validate/preview/simulate/diff/blast-radius/stage/apply/rollback
        |
        +-----> admin/store.py (versioned connections/policy/catalog snapshots)
        +-----> config_reload.reload_config() (same swap as /admin/reload-config)
        +-----> audit trail (config.governance events)

Authenticated agent catalog search
        |
        +-----> resolve principal policy (connection/table/column)
        +-----> filter catalog entries + relationship endpoints
        +-----> tokenize/rank/count/budget the already-authorized view only

Catalog-governance reviewer (catalog:review/edit/approve/reject/publish/rollback)
        |
        v
catalog/governance.py — edit/approve/reject/publish/rollback state machine
        |
        +-----> catalog/repository.py (same CatalogFileRepository lock 32A uses)
        +-----> audit trail (catalog.governance events)

Operator semantic-memory pipeline (disabled by default)
        |
        +-----> bounded row-free schema reflection -> hashed snapshot/diff
        +-----> atomic catalog refresh -> selective stale marking
        +-----> strict offline manual batch -> quarantined inferred drafts
        +-----> no model network adapter and no automatic publication
```

The agent/client, all request fields, natural-language intent, JWTs, API keys
presented by callers, and database row values are untrusted input. QueryGate's
process, policy/configuration files, deployment secrets, reverse proxy, JWKS
configuration, Redis, and audit destination are controlled by the operator.
Database servers are trusted to enforce their own permissions but are not
trusted to return client-safe error messages.

## 3. Assets

- Database credentials and connection topology.
- Secret-backend credentials (e.g. a Vault token) and resolved connection
  secrets, from load through use.
- Database schema, row data, aggregates, and sensitive column existence.
- Curated schema-catalog metadata (descriptions, relationship hints,
  sensitivity labels) layered on top of reflected schema.
- Semantic-catalog provenance, verification state, schema fingerprints/diffs,
  quarantined draft proposals/generation records, and the confidentiality of
  policy-hidden entries during retrieval.
- Catalog-governance review state and version history — every proposal's
  review status/transitions and every publish/rollback's actor attribution
  and durable record.
- Principal identity, claims, scopes, and per-principal policy decisions.
- Availability of QueryGate and the databases behind it.
- Configuration integrity, particularly connection and policy changes.
- Config-governance version history — every staged/applied/rolled-back
  connections/policy/catalog snapshot and its actor attribution.
- Audit-event integrity, availability, and correlation metadata.
- Product-guide integrity and version freshness; caller-scoped guide context
  must not become an oracle for deployment configuration or hidden resources.

## 4. Attacker capabilities

The primary attacker is a remote caller that can invoke REST or MCP, possibly
with a valid low-privilege credential. This includes an otherwise legitimate
agent whose prompt or tool context has been injected. The attacker may:

- submit arbitrary JSON values and every valid combination of query-AST fields;
- enumerate identifiers and compare allowed results or error behavior;
- replay requests and run them concurrently;
- possess a valid token for another tenant, environment, issuer, or audience;
- attempt to induce database, driver, timeout, Redis, or audit failures; and
- access a developer's browser while a local MCP server is reachable.

An attacker with write access to QueryGate's policy files, environment,
container, database credentials, reverse proxy, or host is outside the remote
caller model. Those are privileged operator boundaries and require normal host,
CI/CD, and secrets-management controls.

## 5. Threats and controls

| ID | Threat | Implemented controls | Verification |
|---|---|---|---|
| QG-01 | Raw SQL or SQL injection | No raw-SQL field or endpoint; Pydantic forbids extra AST fields; identifiers resolve to reflected SQLAlchemy objects; predicate values use binds | `test_raw_sql_rejected_on_every_query_endpoint`; security test `test_predicate_payload_is_bound_data_not_executable_sql`; REST/MCP malformed-input boundary fuzzing proving a smuggled `sql`/`raw_sql`/`query`/... field (and every other malformed shape) is rejected before execution (`test_malformed_input_fuzzing.py`, TODO item 36 phase 2a) |
| QG-02 | Table/column policy bypass through filters, joins, grouping, having, ordering, or ranking | Policy gathers every qualified reference before reflection; deny wins; matching is case-insensitive | Security parametrization `test_denied_column_cannot_be_used_for_inference` and denied-table smuggling test |
| QG-03 | Implicit/undeclared table injection | Every referenced table must be the `from` table or an explicit join; each join must reference the joined table and connect to the existing graph | Security test `test_undeclared_table_reference_is_rejected_before_reflection`; schema-validation join tests |
| QG-04 | Schema discovery leaks hidden resources | Connection listing, direct access, table listing, and column description resolve the caller's policy; hidden connections return the same not-found shape as unknown ones | `test_connection_visibility.py`; REST/MCP principal-scoping integration tests |
| QG-13 | Curated schema-catalog metadata discloses a hidden table/column | Catalog entries are display-only and never bypass policy; a denied column is excluded from `describe_table` before catalog lookup happens; a relationship hint pointing at a policy-denied table is dropped from the response | `test_catalog_relationship_hint_cannot_disclose_a_denied_table`; catalog unit/integration tests |
| QG-05 | Cross-principal or cross-tenant confusion | Immutable request-local `Principal`; per-principal policy resolution; claim-derived mandatory row filters; MCP caller stored in a reset `ContextVar` | Security principal-isolation and aggregate row-filter tests; policy-loader and MCP tests |
| QG-06 | Authentication spoofing or token confusion | Constant-time API-key comparison; JWT signature, algorithm, issuer, audience, expiry, required subject, and configured JWKS verification; anonymous bypass only in local/development when no real authenticator is configured | `test_auth.py`, `test_jwt_auth.py`, REST/MCP authentication integration tests |
| QG-07 | Credential, row, predicate, or backend-detail leakage | Public connection DTO has no credential field; unexpected REST/MCP/batch errors are generic; persisted audit schema excludes SQL, params, intent, exception text, and rows; explain/audit SQL is parameterized by default | `test_credential_redaction.py`, `test_audit.py`, security public-error tests; malformed-input boundary fuzzing asserting no traceback/path/driver/credential leaks in a REST or MCP rejection body (`test_malformed_input_fuzzing.py`, TODO item 36 phase 2a) |
| QG-08 | Oversized or abusive requests/results | AST depth/width/join/top-N/batch caps; server-side row limits; hard serialized-row byte ceiling, including a single oversized row; database timeout; per-connection concurrency, including a caller-selected `queue_mode`/`wait_timeout_seconds` that can only shorten the operator's `concurrency_wait_seconds` ceiling, never lengthen it; `max_queue_depth`/`max_queue_depth_per_principal` bounding how many callers may wait at once (cross-replica when `concurrency_backend: redis`), so an unbounded waiting queue can't itself become a resource-exhaustion vector; optional Postgres pre-execution `EXPLAIN`-based row/cost estimate rejection before a likely full scan or join explosion runs | Policy/service tests, real timeout tests, security oversized-row test, `test_postgres_cost_estimation.py`, `test_caller_cannot_extend_the_operators_concurrency_wait_ceiling`, `test_unbounded_waiting_queue_is_capped_not_a_dos_vector`, `test_max_queue_depth_per_principal_prevents_one_caller_starving_another` |
| QG-09 | Cross-connection access | Both connections must be visible to the principal and share the resolved `join_group`; the join uses the primary engine and declared physical database mapping. Being in the same `join_group` only settles whether the join is allowed — a joined table's column masks, mandatory row filters, and table/column deny-list are enforced from BOTH its own connection's Policy and the primary connection's, never one exclusively (item 156), the same per-connection resolution the catalog sensitivity-label approval trigger uses (item 155). Reflection through the primary engine assumes every `join_group` member is the same physical server instance (a same-instance cross-database sibling), not a genuinely separate host — enforced in two layers (item 170): `ConnectionRegistry.from_entries` rejects a `ConnectionProfile.join_group` whose ENABLED members resolve to different hosts (or the same host with different ports) at config-load/hot-reload time, and `resolve_query_table_connections` independently enforces the same host agreement at REQUEST time against the fully-resolved join_group — required because `Policy.join_group` (default, per-connection, or per-principal, set in a separate file the config-load check never sees) can unite two connections the config-load layer never compared. Both layers share one `(host, port)`-parsing function that never returns the full connection string, is robust to a driver that packs its host into a `?host=` query parameter, and strips an un-encoded `@` in a password (which SQLAlchemy's URL parser would otherwise fold into the parsed host) before it can ever reach an error message | Cross-connection schema tests, including hidden-connection denial; `test_policy_validation.py`/`test_column_masking.py`/`test_compiler.py`/`test_service.py` cross-connection-join coverage (item 156); `test_cross_connection_self_join_reflects_each_alias_against_its_own_connection` (`test_schema_validation.py`, item 166 — a self-join across two connections previously let one alias's reflection silently win a name-only memo slot, so the OTHER alias read that connection's data while its own Policy was still resolved against its own connection, under-enforcing the losing connection's masks/filters/deny-list); `TestJoinGroupHostValidation` (`test_connections_registry.py`, item 170, 13 tests — the config-load-time layer, including the credential-leak and query-parameter-host regressions) and `test_policy_join_group_spanning_different_hosts_is_rejected`/`test_policy_join_group_on_the_same_host_with_one_port_omitted_is_allowed` (`test_schema_validation.py`, item 170 — the request-time layer, covering the `Policy.join_group`-only case the config-load layer cannot see and its own port-omission false-positive guard) |
| QG-10 | Unauthorized configuration changes | `/admin/reload-config` requires `admin:reload-config`; the config-governance API (`/admin/config/*`) separately requires `admin:config:write` for validate/preview/stage/apply/rollback and `admin:config:read` for history/inspection; every path fully validates new content before atomic registry/policy replacement. The governed documents are connections, policy, catalog, and — since item 48 phase 2 — query templates (`templates.yaml`): a curated query template is authored only through this staged/validated/re-validated-on-apply/rollbackable path, never a direct mutation endpoint, so it can never publish itself | REST reload scope tests, config-reload tests, `test_config_governance_write_endpoints_require_write_scope`, `test_config_governance_read_endpoints_require_read_scope`, `test_query_template_authored_through_governance_becomes_invocable_then_rolls_back`, `test_staged_template_is_not_live_until_a_separate_apply` |
| QG-11 | Browser-driven DNS rebinding against local MCP | MCP validates `Host` and, when present, `Origin`; protection is enabled by default with loopback hosts allowlisted | Security test `test_mcp_rejects_unapproved_host_header` |
| QG-12 | Audit data becomes a new exfiltration channel | Narrow versioned event schema, explicit normalization without values, mode `0600`, append-only application writes, optional `fsync` | `test_audit.py` |
| QG-32 | Audit trail is silently altered, truncated, or reordered after the fact to hide what an agent did | Optional tamper-evident hash-chained ledger (`AUDIT_SINK_BACKEND=jsonl_chained`, item 91): each redaction-safe event is wrapped in a chain envelope linking it to the previous record's hash, so any edit, deletion, reordering, or insertion breaks verification (`querygate-audit verify`). Keyed (`AUDIT_LEDGER_HMAC_KEY`) the chain is HMAC-SHA256 and unforgeable without the key; unkeyed it is SHA-256 and tamper-evident relative to an externally anchored head. Per-query portable receipts (`querygate-audit receipt`) prove one event's chain position without the rest of the ledger. The chain adds only a sequence number and hashes — no new event data, redaction guarantee unchanged | `test_audit_ledger.py`, `test_audit_cli.py` |
| QG-14 | Secret-backend failure or misconfiguration discloses a Vault token or backend response text | `SecretResolver.resolve` errors carry only the reference being looked up and the exception type, never the configured token or the backend's own error/response text; a resolved secret value is never returned by any REST/MCP response, matching the existing credential-redaction guarantee | `test_vault_resolver_error_never_leaks_token_or_backend_response_text`, `test_vault_resolved_secret_never_appears_in_connection_listing_or_errors` |
| QG-15 | A config-governance version that stops validating (e.g. an env var/Vault path it depends on disappears between staging and applying) gets silently activated anyway | `apply` re-validates a version's content immediately before activating it, regardless of whether it validated when staged; a failed re-validation leaves the active version and pointer untouched and is recorded as a rejected audit event | `test_apply_rejects_a_version_that_no_longer_validates`, `test_invalid_staged_version_is_rejected_not_silently_applied` |
| QG-16 | Product-guide search, diagnostics, or caching disclose another principal's connections, policies, secret references, or hidden schema identifiers | Static search indexes only packaged public topics; live access context is authorized and assembled separately for each request; no live context cache exists; admin inspection requires `admin:config:read` and reconstructs an allowlisted redacted projection rather than filtering raw YAML afterward | `test_product_guide_security.py`, `test_redacted_configuration_excludes_secrets_policy_names_and_other_principals`, REST/MCP guide integration tests |
| QG-17 | Semantic catalog poisoning, provider output, stale guidance, or search/ranking/count/relationship traversal discloses a policy-hidden table/column or changes enforcement | Every published entry has stable provenance, explicit status/confidence/schema freshness, and server-derived precedence; rejected/archived content is excluded and stale content is labeled; verified entries cannot be overwritten by lower-precedence proposals and merging cannot change sensitivity. Provider mode defaults to disabled and only strict offline manual imports exist: their schema-bound output is stored in a separate inferred-draft queue whose model has no policy/sensitivity/sampling fields and which retrieval never indexes. Row-free refresh atomically stales only affected entries/proposals, rebinds unaffected entries, and logs only failure types. Principal policy filters published candidates and both relationship columns before tokenization, ranking, counting, or byte budgeting, removing free-form fields that echo exact hidden identifiers; public citations hash evidence references and omit actor/model identities; schema snapshots contain structure and comment hashes, never rows or raw comments | `test_catalog_retrieval.py`, `test_schema_memory.py`, `test_catalog_generation.py`, `test_catalog_refresh.py`, `test_semantic_memory_benchmark.py`, `test_manual_provider_output_cannot_publish_itself_or_change_verified_content`, `test_schema_refresh_failure_log_never_copies_raw_driver_error`, REST/MCP catalog-search integration tests |
| QG-18 | A caller with partial catalog-governance privilege publishes without authorized approval, silently overwrites already-verified content, a rollback discards a change made after the one it targets, an import corrupts an unrelated connection's history, or a deletion leaves a dangling reference an unauthorized caller could exploit or that would crash schema validation (TODO items 32B-1/32B-2) | Nine independent least-privilege scopes (`catalog:generate/review/edit/approve/reject/publish/rollback/export/delete`) gate every mutation; none is implied by another. A proposal's `review_status` starts `pending` and can only reach `published` through a separate, actor-attributed `approved` transition — a draft can never publish itself, and repeated/out-of-order/invalid transitions fail with no partial mutation. Publishing an approved proposal onto a table/column/relationship that already carries different, human-verified content for the same field is always rejected as a reviewable conflict, never silently overwritten; a draft's content model has no sensitivity/sampling/policy/mandatory-filter field, so publication cannot touch any of those regardless of what is approved. Rollback is idempotent, refuses if the entry has changed since the targeted publish, and refuses a table-creation rollback while later publishes still depend on it. Import (destructive, connection-scoped) re-numbers/de-duplicates file-global version and generation ids against the target catalog's current content so it can never collide with or corrupt an unrelated connection's history, and only ever replaces the connection named in the request. Deletion only ever removes terminal-state records (rejected, or published-and-rolled-back) and cascades a publish record's paired rollback record together so no dangling `rolled_back_version_id`/`published_version_id` reference can ever be persisted; a proposal whose publish is still live can never be deleted. Every generation and state transition is a redaction-safe `catalog.governance` audit event (ids/actor/outcome only, never draft text or raw YAML) | `test_catalog_governance.py`, `test_catalog_governance_rest.py`, `test_catalog_governance_write_scopes_are_independent`, `test_catalog_governance_review_endpoints_require_review_scope`, `test_catalog_governance_endpoints_reject_unauthenticated_callers`, `test_a_draft_proposal_cannot_publish_itself`, `test_catalog_export_and_delete_scopes_are_independent`, `test_catalog_delete_scope_alone_cannot_export_or_import`, `test_catalog_export_scope_alone_cannot_delete`, `test_import_remaps_version_ids_and_cross_references_to_avoid_collision`, `test_delete_published_proposal_requires_rollback_first` |
| QG-19 | Draft-aware policy simulation (`POST /admin/config/simulate`, TODO item 39) becomes a secret-existence oracle, discloses static row-filter values/claims/query predicates/resolved secrets, leaks hidden policy metadata for a table the target principal can't see, persists a candidate as a real version, or lets concurrent simulations interfere with live traffic or each other | `simulate` requires both `admin:config:read` and `admin:config:write` together (neither alone is sufficient), unlike every other `/admin/config/*` action which needs only one; candidate documents are loaded through the same loaders/cross-validation as `/validate` into a temporary, request-scoped registry/policy/catalog context that is never installed as a process global and is discarded when the request completes — the live registry, policy store, and config-version store are provably untouched, so concurrent production requests and other simulations can't observe or corrupt each other. The response schema (`CandidatePolicySimulation`) structurally excludes resolved secret values, static mandatory-filter values, supplied claim values, query predicate values, and compiled SQL; a table the candidate policy hides from the target principal never contributes its mandatory-filter identifiers to the response, even when the caller explicitly names that table. An invalid candidate fails closed with a generic message pointing at `/validate` for detail, rather than echoing the offending content, and is still recorded as a redaction-safe rejected audit event | `test_candidate_simulation_uses_isolated_context_and_redacts_values`, `test_candidate_simulation_does_not_reveal_filters_for_denied_table`, `test_candidate_simulation_masks_invalid_candidate_content`, `test_candidate_simulation_uses_draft_without_persisting_or_changing_live_policy`, `test_config_governance_write_endpoints_require_write_scope`, `test_config_governance_read_endpoints_require_read_scope` |
| QG-20 | Semantic access diff (`POST /admin/config/diff`, TODO item 40 phase 1) becomes a secret-existence oracle, discloses static mandatory-filter values/resolved secrets/query predicate values/raw YAML, persists the candidate as a real version, or lets a concurrent diff interfere with live traffic | `diff` requires both `admin:config:read` and `admin:config:write` together (neither alone suffices), for the same read-detail-plus-write-resolution reasoning as `simulate`. Both the active and candidate snapshots load through the same isolated, request-scoped registry/policy/catalog context `simulate` uses (`_load_isolated_candidate_context`), never installed as a process global and discarded when the request completes — the live registry, policy store, and config-version store are provably untouched and nothing is persisted. The response schema (`SemanticAccessChange`) structurally carries only non-sensitive resolved values (guardrail numbers, `visible`/`hidden`/`absent`, join-group names, mandatory-filter source kind/claim name); static filter values, resolved secrets, connection strings, predicate values, and raw YAML are never placed in it. An invalid candidate fails closed with the same generic message pointing at `/validate` and is recorded as a redaction-safe rejected `diff` audit event. Per-principal override changes are flagged as `analysis_incomplete` rather than resolved (phase 2) | `test_config_semantic_diff.py`, `test_diff_reports_guardrail_change_without_mutating_live_state`, `test_diff_masks_invalid_candidate_and_audits_rejection`, `test_mandatory_filter_added_is_tightening_removed_is_loosening_without_values`, `test_diff_endpoint_reports_semantic_changes_without_persisting`, `test_config_governance_write_endpoints_require_write_scope`, `test_config_governance_read_endpoints_require_read_scope` |
| QG-21 | Admin connection-status API (`GET /admin/connections`, TODO item 43 phase 1) discloses database topology or a connection string, echoes a raw driver error that embeds a host/port/database/username, or is readable without the intended privilege | The endpoint is gated by a dedicated `admin:connections:read` scope, independent of the config scopes, so operational-health visibility is not implied by any other admin privilege and an anonymous caller is rejected. The response model (`ConnectionStatus`) carries only credential-free fields already exposed by `PublicConnectionInfo` (`connection_id`/`dialect`/`enabled`) plus derived health (`status`/`last_checked`/`last_success`/`latency_ms`/`schema_reflected`); no connection string is present. A failure is reported only as a stable `failure_category` (`authentication`/`unreachable`/`timeout`/`error`) derived from the exception *type*, never its message, so a driver error embedding a host/username/password cannot leak — the raw error remains in stdout logs only, matching `/health`'s existing non-disclosure of topology to its unauthenticated probe | `test_admin_connections_status_requires_its_own_scope`, `test_response_is_sorted_and_never_leaks_raw_error_or_credentials`, `test_classify_failure_never_uses_the_message`, `test_lists_credential_free_per_connection_status` |
| QG-23 | Admin "test now" connection probe (`POST /admin/connections/{id}/test`, TODO item 43 phase 2a) is triggered without the intended privilege, echoes a raw driver error/connection string like QG-21, or is abused as a repeated-connection-attempt resource-exhaustion vector against the target database | Gated by its own `admin:connections:test` scope — independent of `admin:connections:read`, so passive status visibility does not imply the ability to trigger a live probe, and vice versa. The probe reuses `HealthMonitor`'s exact ping/classification path and returns the same credential-free `ConnectionStatus` model as QG-21, so it inherits the identical non-disclosure guarantee. Each connection can be manually probed at most once per `AppConfig.admin_connection_test_cooldown_seconds` (default 10s); a request inside that window is rejected with `429`/`Retry-After` before a second real connection attempt is made, rather than being queued or silently throttled. Every probe attempt — successful or rate-limited — is recorded as a redaction-safe `connection.probe` audit event (connection id, actor, probe result/failure category, never a raw driver error or connection string) | `test_admin_connections_test_now_requires_its_own_scope`, `test_test_now_success_updates_status_and_never_leaks_raw_error`, `test_test_now_is_rate_limited_per_connection`, `test_test_now_persists_audit_event_without_raw_error` |
| QG-22 | Policy-change blast-radius analysis (`POST /admin/config/blast-radius`, TODO item 41) becomes a secret-existence oracle, discloses static filter values/resolved secrets/raw YAML, persists the candidate as a real version, performs unbounded work by fanning a diff out across every conceivable principal, or silently under-reports impact when a bound is reached | `blast-radius` requires both `admin:config:read` and `admin:config:write` together, identical to `diff`'s reasoning — it echoes resolved policy detail while resolving caller-supplied config/secret references. It is built entirely from `diff`'s own primitives: the same isolated, request-scoped registry/policy/catalog context (never installed as a process global, discarded when the request completes) and the same `compute_access_diff` classification logic, called once at the connection baseline and once per principal that has an explicit `principals:` override in either snapshot — a principal with no override is provably identical to the baseline by construction, so it is never separately fanned out. Work is bounded on two axes, each with its own explicit cap: at most 100 configured principals are individually evaluated, and each principal's own change list is capped like `diff`'s; either cap being reached sets `analysis_incomplete` with a specific, human-readable reason rather than silently omitting impact. `highest_risk` ranks only access-*expanding* (loosening) changes — mandatory-filter removal ranked above newly visible connections/tables/columns, ranked above a loosened guardrail — and is itself capped at 25 entries with the same incomplete-reason behavior if truncated. The response schema structurally excludes static filter values, resolved secrets, connection strings, and raw YAML, matching `SemanticAccessDiff`'s existing redaction guarantee at every nesting level (baseline, each principal's own changes, and `highest_risk`). An invalid candidate fails closed with the same generic message pointing at `/validate` and is recorded as a redaction-safe rejected `blast_radius` audit event | `test_blast_radius.py`, `test_blast_radius_reports_baseline_and_configured_principal_impact`, `test_blast_radius_masks_invalid_candidate_and_audits_rejection`, `test_blast_radius_endpoint_finds_targeted_expansion_under_an_overall_tightening`, `test_blast_radius_never_leaks_static_filter_values_or_yaml`, `test_principal_cap_marks_analysis_incomplete_and_bounds_work`, `test_highest_risk_cap_marks_analysis_incomplete`, `test_config_governance_write_endpoints_require_write_scope`, `test_config_governance_read_endpoints_require_read_scope` |
| QG-24 | Non-admin "my access" portal (`/access/`, `GET /help/my-access`'s new `connection_access` field, TODO item 45) discloses another principal's effective guardrails or mandatory-filter claim readiness, leaks a mandatory-filter value/claim value, discloses filter metadata for a table the caller's own policy hides, or the static shell smuggles in admin-only navigation/actions/endpoints | `connection_access` is built from the caller's own already-authenticated `Principal` only — there is no target-principal parameter, unlike `/admin/config/simulate`, so a caller can never request another subject's guardrails. Guardrails and mandatory-filter readiness reuse item 39's exact typed, redaction-safe models (`EffectiveGuardrails`/`MandatoryFilterReadiness`): guardrail numbers and table/column/claim-*name*/source/ready booleans only — never a static filter value or a supplied claim's resolved value. A mandatory filter on a table the caller's own resolved policy denies is excluded before it ever reaches the response, matching QG-19's "don't leak hidden-table filter metadata" reasoning. The `/access/` static shell is served with the same CSP/no-referrer/nosniff/no-store headers as `/admin/`, is public (only the API calls it makes require the caller's own bearer token, identical to `/admin/`'s own posture), and is asserted to never contain admin-only navigation labels, `admin:config:write`, or `/admin/config/`\|`/admin/catalog/` endpoint references | `test_access_summary_reports_effective_guardrails_and_claim_readiness`, `test_access_summary_excludes_mandatory_filter_for_a_table_the_principal_cannot_see`, `test_my_access_reports_per_principal_guardrails_and_claim_readiness`, `test_access_ui_is_served_with_browser_security_headers_and_no_admin_controls`, `test_access_ui_shell_is_served_without_authentication` |
| QG-25 | Policy templates (`GET /admin/config/templates`, `POST /admin/config/templates/render`, TODO item 46) are readable/renderable without the intended privilege, a rendered template embeds a credential/tenant value, or applying a template silently loosens or drops a restriction already present in the caller's draft | `render` requires `admin:config:write` (like `/validate`/`/preview`, since it resolves caller-supplied content meant to be staged); `GET /templates` requires `admin:config:read`, and both reject an unauthenticated caller. Templates are a fixed, code-reviewed Python registry (`admin/templates.py`) — parameters are typed and validated, never inferred from live schema/table names, and no template patch ever contains a connection string, secret reference, or literal tenant value; `render_template` never touches the live registry/policy singletons, the config-version store, or any secret, operating only on the caller-supplied `policy_yaml` text. The merge is monotonically restrictive by construction: numeric guardrail caps take `min(existing, template)`, `allowed_tables` only narrows an existing non-empty allow-list, and `denied_columns`/`mandatory_row_filters` only union onto what is already present — never replacing or removing an existing restriction — with every case where an existing value was kept over the template's own called out by name in the returned `rules` preview rather than silently absorbed. A rendered document is plain `policy_yaml` text with no special privilege: it still must pass the unchanged `/validate` and re-validation-on-apply gates (QG-15) before it can ever be activated | `test_policy_templates.py` (`test_render_missing_required_parameter_is_rejected`, `test_template_never_loosens_an_existing_stricter_cap`, `test_template_never_widens_an_existing_allow_list`, `test_tenant_isolated_reapplication_is_idempotent`, `test_template_never_embeds_connection_strings_or_secrets`), `test_list_templates_requires_read_scope`, `test_render_template_requires_write_scope`, `test_render_template_end_to_end_through_validate_and_stage` |
| QG-26 | A curated query template (`run_query_template` / `POST /query-templates/{id}/run`, TODO item 48) is invoked to exceed the caller's policy, a parameter smuggles a raw identifier/SQL, an invoker enumerates hidden templates/connections, or the audit trail records a parameter value | A template is a stored `StructuredQuery` skeleton, not a SQL string — there is no raw-SQL field anywhere. At invocation the caller's parameters are type/constraint-checked against typed slots and substituted only into value positions, and the *bound result is validated as a real `StructuredQuery` and run through the unchanged `StructuredQueryService`*, so it is subject to the identical policy caps, table/column allow-deny, mandatory row filters, schema checks, and guardrails as an ad-hoc query — a parameter gets no exemption, and a bound query that references a denied column is rejected exactly as an ad-hoc one is. A template is visible/invocable only if its target connection is visible to the principal (item 22); an unknown template and one on a hidden connection return the same non-enumerating not-found, so it can't be used to probe hidden template ids or connection names. Invocations audit distinctly (`operation="run_query_template"`, the template id, and the parameter *names*) but never parameter *values*, which are bound as data and stripped from the query shape like any literal. Templates are declarative, file-configured, hot-reloadable config (as safe as `policy.yaml`, and a template can't exceed policy); as of item 48 phase 2 that config is *governed* — authored only through the staged/validated/rollbackable config-versioning plane (`templates.yaml` is a governed document, see QG-10), never a direct mutation endpoint | `test_query_templates.py`, `test_query_template_api.py` (`test_unknown_and_hidden_templates_are_uniform_404`, `test_audit_records_template_id_and_param_names_never_values`), `test_query_template_cannot_exceed_policy` |
| QG-27 | The on-demand template live-schema check (`POST /admin/config/check-template-schema`, TODO item 83) is triggered without the intended privilege, echoes a raw driver error/connection string, discloses schema of a connection beyond the caller's privilege, or is abused as a repeated-reflection resource-exhaustion vector | Gated by both `admin:config:read` and `admin:config:write` together (like `/simulate` and `/diff`): it reveals live column/table existence (read-like) while resolving caller-supplied template content (write-like), so neither scope alone suffices and an anonymous caller is rejected. It reflects the *currently-live* connections through the same cached `validate_schema` the real query pipeline uses — read-only, never executing the query or returning rows — against a dummy-bound skeleton (placeholder values never change which identifiers are referenced). The per-template result (`TemplateSchemaCheck`) carries only the template id, connection id, a coarse `status`, and the schema-validation message (a missing column/table *name*, which an admin with config access can already see via the schema browser) — never row values, connection strings, or a raw driver error: any database/reflection failure is caught and reported as the generic `unreachable` status, so a connection error embedding host/credentials can't leak. Best-effort by design (an unreachable database is a result, not a `500`), and every invocation is a redaction-safe `check_template_schema` audit event (actor/outcome only) | `test_template_schema_check.py` (`test_unreachable_database_is_best_effort_and_never_leaks_the_driver_error` — asserts the generic message and that host/credential/SQL tokens are absent, adversarially verified to fail if the raw error is surfaced; `test_unknown_connection_is_connection_unavailable`; missing-column/table classification), `test_check_template_schema_never_500s_or_leaks_a_driver_error` (endpoint returns `200` with no driver text in the body when reflection raises), `test_check_template_schema_endpoint_flags_missing_column`, `test_config_governance_write_endpoints_require_write_scope`, `test_config_governance_read_endpoints_require_read_scope` |
| QG-28 | The admin observability overview (`GET /admin/observability/overview`, TODO item 44) is read without the intended privilege, leaks a per-query value / principal / SQL through its aggregates, or implies durable history it cannot truthfully provide | Gated by a dedicated `admin:observability:read` scope (distinct from the config and connection scopes — reading trend aggregates is a different privilege), which gates the *whole* overview including the per-connection breakdown; an anonymous or wrong-scope caller is rejected `403`. The response is assembled only from the in-process Prometheus registry's labels, which are low-cardinality and public by construction (`connection` is a `PublicConnectionInfo` id; `reason`/`outcome`/`quota_kind` are fixed enum buckets — see `metrics.py`) — it carries no SQL, predicate value, row, principal, table, or column, the same invariant as `metrics.py`/`audit/logger.py`, and the response model is `extra="forbid"` with a fixed aggregate shape so no free-form value field can appear. It never overstates what it knows: `source="process_snapshot"`, `durable=false`, `since`, and a `note` make explicit that counters are cumulative-since-process-start and per-replica under the default backends, so it can't be mistaken for a durable cross-replica time-series it does not own | `test_observability_api.py` (`test_overview_requires_the_observability_scope`, `test_overview_is_returned_with_the_scope_and_labeled_non_durable`, `test_overview_carries_only_low_cardinality_aggregates_no_values`), `test_observability.py` (pure-aggregator unit coverage) |
| QG-29 | A caller reconstructs a *denied* value indirectly (inference) — referencing a denied column in a non-`select` clause, or singling out an individual by aggregating over a razor-thin filter — rather than selecting it directly | Column allow/deny is enforced on **every** column reference in **every** clause (the single canonical reference visitor `validation/schema_validation.py`'s `iter_column_refs`, consumed by `validation/policy_validation.py` — TODO item 96), not just `select`: where, group_by, having, order_by, top_n partition/order, join `on`/`extra_on`, a join `condition` predicate tree (item 103), scalar-function args, `CASE` when/then/else, aggregate/`percentile_cont`/`string_agg` columns, predicate `col_fn` and `value_col`, every node of a computed `Expression` (item 100), a window's `arg`/`PARTITION BY`/`ORDER BY` (item 101), and the operand of an `extract`/`date_add` date expression (item 102) — and it runs **once per scope**, not once per request: every nested `IN (subquery)` (item 97) and every set-operation arm (item 104) is enumerated as its own scope by `iter_query_scopes` and gets the identical treatment against its own table/alias map, so a denied column cannot be reached from a second SELECT either. Nesting is no defense: since item 100 an expression MAY nest (functions, arithmetic, `CASE`), so the guarantee is that the one canonical walk recurses through every level and **fails closed** on an unrecognized node, rather than the AST forbidding depth. (The older one-level `ScalarFunctionCall` spelling still forbids nesting; the general `Expression` is depth-capped instead, by `max_expression_depth`/`max_expression_nodes`.) `test_denied_column_cannot_be_used_for_inference` is parametrized across all of them. For the aggregate-singling-out vector, the optional `Policy.min_group_size` (TODO item 88) makes the compiler inject `HAVING count(*) >= k` into every aggregate query, suppressing any group backed by fewer than *k* rows. Because that floor counts *joined* rows, a join able to match many rows per row would inflate it — so while the floor is set, such a join is **refused** on an aggregate query rather than silently answered (TODO.md item 118, measured 2026-07-27 with a plain equality join). The refusal is scoped to the shapes that break it: a join onto the target's primary key or a unique column matches at most one row, cannot inflate a count, and stays allowed — and while it is set, **aggregate window functions are rejected** (TODO item 101), since a window aggregate produces no group for that floor to filter and `COUNT(*) OVER ()` would report a below-*k* count with no column projected at all. For the multi-query form — differencing two independently k-compliant aggregates by walking a predicate constant — `Policy.max_shape_repeats_per_window`/`max_aggregate_queries_per_window` (TODO item 179, `execution/disclosure_budget.py`) cap, per (principal, connection, declared purpose, table) over a rolling window, how many times one aggregate *shape* may be re-run and how many aggregate queries may touch one table at all; because the recorded shape is predicate-literal-free, a probe walking a constant is one shape re-sent N times, so re-runs are the signal. **Residual (item 186, 2026-08-12):** the shape fingerprint does not canonicalize a select-alias reference or a cte rename, so a prober can mint a fresh shape bucket per probe and the per-shape cap does not bind; `max_aggregate_queries_per_window` is the load-bearing cap until that lands. Both caps are off by default, both require `min_group_size` to also be set, and only `execute` spends budget (`explain`/`verdict` return no rows). This **bounds** the attack, it does not close it: a caller inside its budget still differences successfully, and closing it properly needs query-set auditing or differential privacy. The full analysis — including the residuals identifier allow/deny structurally cannot close (derived/correlated permitted columns, underlying-data correlation, multi-query differencing, existence probing) and their in-depth mitigations — is `docs/INFERENCE_RISKS.md` (TODO item 55) | `test_adversarial_security.py` (`test_denied_column_cannot_be_used_for_inference` across every clause, `test_min_group_size_closes_the_single_row_aggregate_singling_out`, the R1/R3 residual demonstrations), `test_compiler.py::TestMinGroupSize`, `test_disclosure_budget.py` + `test_disclosure_budget_e2e.py` (item 179's bound, including an end-to-end salary-differencing probe cut off partway through), `test_sqlite_end_to_end.py` (real group suppression), `test_subquery_boundary.py` + `test_set_operation_boundary.py` (the same rules proven per scope container) |
| QG-30 | Config change-set export/import (`POST /admin/config/export`, `POST /admin/config/import`, TODO item 47) is triggered without the intended privilege, discloses active config content the caller never submitted, becomes an ungoverned shadow config store that bypasses staging/validation, silently imports onto a drifted base, exhausts memory with an oversized upload, or persists a credential in the browser | Both endpoints require `admin:config:write` (a change-set bundle is a config-mutation carrier); a read-only caller is rejected `403`. Export echoes **only** the documents the caller themselves submitted — an unset document is never resolved into the bundle — so it can never be used to read the active connections/policy content, and it stamps the current active version's id and a content fingerprint (never the active content) so drift can later be detected. Import is validation-only: it never stages or persists anything, it re-validates the resolved candidate through the exact same loaders the live pipeline and `/validate` use, and staging still goes through the unchanged `/versions` endpoint — there is one governed mutation path, not a shadow store. A bundle whose recorded base fingerprint no longer matches the target's active version is flagged `stale_base` with the specific documents that moved, rather than silently importing onto a drifted base. The upload is bounded by `AppConfig.config_bundle_max_bytes` (default 1 MiB) and an oversized bundle is a clean validation failure, never an OOM. The import response is content-free (a change signal, not YAML). In the browser, only the *policy* document is ever written to `localStorage`; `connections.yaml` (which can carry a literal credential), secret references, and bearer tokens are never persisted there — full-config recovery uses the explicitly downloaded file | `test_config_change_set.py` (export excludes inherited content, stale-base detection, oversized rejection, import never persists), `test_admin_config_governance.py` (`test_export_import_roundtrip_then_stage`, `test_import_surfaces_stale_base_after_active_moves`, `test_import_rejects_oversized_bundle`), `test_adversarial_security.py::test_config_governance_write_endpoints_require_write_scope`, `test_admin_ui.py::test_local_draft_recovery_is_policy_only_never_credentials` |
| QG-31 | A browser-based attack (most concretely an XSS) against the admin control plane (`/admin/`, `/access/`) steals the operator's bearer token — or another persisted credential — out of browser storage | The bearer token is held only in an in-memory variable for the tab's lifetime — never in `localStorage`, `sessionStorage`, or a cookie — so there is no stored token for injected script to read or exfiltrate, and a reload requires re-authentication. This removes the highest-value XSS outcome (session-token theft from storage) **by construction**, independent of whether any XSS bug exists. Two further controls reduce the XSS surface itself: a strict Content-Security-Policy on every control-plane response (`default-src 'self'; script-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'`, plus no-store/nosniff/no-referrer), which blocks inline/external script injection execution, and the single-page app escapes untrusted content before rendering it. The only value the control plane persists in the browser is an in-progress *policy* draft in `localStorage` (QG-30/item 47) — never `connections.yaml`, a credential, a secret reference, or a token. Scope boundary (no overclaim): this eliminates *token theft from browser storage*, not XSS in general | `test_admin_ui.py::test_local_draft_recovery_is_policy_only_never_credentials` (shipped script has no `sessionStorage` use and no token storage key; the one `localStorage` write persists only the policy document), `test_admin_spa_is_served_with_browser_security_headers` (CSP/`script-src 'self'`/no-store/nosniff/no-referrer on `/admin/` shell + assets), `test_access_ui_is_served_with_browser_security_headers_and_no_admin_controls` |
| QG-35 | The in-query human-in-the-loop approval gate (TODO item 92) is bypassed — an agent forges an approval token, replays a token issued for a *different* (cheap) query against an expensive one, replays an old token indefinitely, or self-approves its own sensitive/expensive read | An approval token is an HMAC-SHA256 signature over `{query fingerprint, approver, expiry}` (`execution/approval.py`), verified with a constant-time compare and **fail-closed** on any missing-key/forged/expired/mismatched/malformed input — a forged or tampered token never verifies without the server's `APPROVAL_TOKEN_HMAC_KEY`. The token is bound to a SHA-256 fingerprint of the **entire** query AST, so a token issued for one query is rejected against any other (a one-character change to a predicate value or column changes the fingerprint); it carries a short expiry, so it cannot be replayed indefinitely. Issuing a token requires the `query:approve` scope, which is deliberately distinct from query execution (a "Query Approver" role, `docs/SCOPE_CATALOG.md`), so the querying agent cannot approve its own read — separation of duties by scope. The gate is opt-in per policy and adds only a pre-execution *pause*: it never widens the input surface or relaxes any allow/deny or cap | `test_approval.py` (`test_forged_signature_is_rejected`, `test_token_is_rejected_for_a_different_query_fingerprint`, `test_expired_token_is_rejected`, `test_token_is_rejected_under_a_different_key`, `test_malformed_tokens_fail_closed`, `test_verification_without_a_key_fails_closed`, `test_gate_admits_a_token_for_a_different_query_never`, `test_approve_endpoint_requires_scope`, e2e `test_execute_pauses_then_admits_after_approval`) |
| QG-33 | The server-side encrypted config-draft store (`POST/GET/GET-by-id/DELETE /admin/config/drafts[/{id}]`, TODO item 47 phase 2) is reached without the intended privilege, one principal reads/deletes another principal's saved draft (which may carry a literal connection credential), a draft is ever persisted as plaintext, the store becomes a second config-mutation path, or an admin can accumulate unbounded server-side storage | `save`/`load`/`delete` require `admin:config:write` (the same carrier-of-a-credential reasoning as QG-30's export/import); `list` is metadata-only (`DraftSummary` — id/description/timestamps/`contains_connections`, never document content) so `admin:config:read` alone suffices, matching `GET /versions`. Every draft's actual document content (`ConfigChangeSetBundle`, the identical phase-1 export shape) is Fernet-encrypted at rest (`admin/draft_store.py`) under a key derived from `AppConfig.draft_store_encryption_key`; an empty key **disables the subsystem** rather than ever writing plaintext, reported honestly as a `503`. Ownership is enforced per-principal at the storage layer, not just the route: loading or deleting a draft that exists but belongs to a different principal raises the exact same `NotFoundError` as an unknown id, so the endpoint can never become a draft-id enumeration or cross-principal existence oracle — proven end-to-end with two distinct JWT principals, not just unit-level. **Residual, not a defect (documented deliberately, not silently left untested):** this isolation is only as fine-grained as the deployment's identity model — under this codebase's static `api_keys`, every configured key shares one `api_key_subject`, so two admins each holding their own key see and can delete each other's drafts; JWT auth is required for real per-admin isolation, the same limitation item 45's `/help/my-recent-denials` already carries — and, since item 126, the same shared subject is also the *rate-limit* bucket for that endpoint's cooldown: two callers sharing a static API key share one cooldown window, so one caller's request can 429 the other's (an availability coupling, never a bypass — see `help/personal_denials.py`'s `PersonalDenialsCooldown` docstring). That cooldown is also, separately, per WORKER PROCESS, not global: under `AppConfig.num_of_workers > 1` or multiple replicas behind a load balancer, each process holds its own independent cooldown state, so the effective ceiling is (workers × replicas) calls per window — the same limitation `execution/quota.py`'s in-process limiter documents and solves with an optional Redis-backed variant, not yet warranted by this endpoint's traffic. Retention is enforced by `AppConfig.draft_store_max_drafts_per_principal` (default 20, rejecting a new save over the cap rather than silently evicting an old draft) and opportunistic expiry pruning on every save/list call (`AppConfig.draft_store_retention_seconds`, default 7 days) — no unbounded accumulation, no background cron needed. Loading a draft only ever returns it to the *existing* validate/stage/apply flow; the store has no path of its own to becoming an active version | `test_draft_store.py` (encrypted round-trip, plaintext-never-on-disk via direct file-content assertion, cross-principal `NotFoundError` on load/delete — mutation-verified against both the ownership check and the expiry check, per-principal listing isolation, the per-principal cap, wrong-key decryption failure, disabled-store `ServiceDisabledError`), `test_draft_store_api.py` (`test_saved_draft_never_leaks_another_principals_draft` — two-JWT proof, `test_static_api_keys_share_one_identity_so_drafts_are_not_isolated_between_them` — the documented API-key residual, `test_endpoints_report_503_when_store_disabled`, `test_max_drafts_per_principal_cap_is_enforced_over_rest`, `test_save_and_load_are_recorded_in_the_audit_stream_without_document_content`), `test_adversarial_security.py::test_config_governance_write_endpoints_require_write_scope`/`test_config_governance_read_endpoints_require_read_scope` |
| QG-34 | The caller-facing verdict endpoint (`POST /{connection}/query/verdict`, MCP `run_structured_queries(mode="verdict")`, TODO item 133) becomes a schema/policy discovery oracle by letting a denial distinguish "on the caller's policy deny list" from "doesn't exist in the schema" from "references a join connection you can't see", by revealing the compiled plan/touched-table list by default, by re-exposing the same category through a different caller-facing surface, or by being cheaper to abuse than a real query — extending the same oracle class QG-19/QG-24 already close on the admin simulation and "my access" surfaces | `validate_policy` runs strictly before `validate_schema` in the shared `_validate_and_compile` seam, so a denial's real exception type/message would let a caller enumerate identifiers and reconstruct both the schema and the policy boundary one query at a time; the verdict endpoint's own `except (PolicyViolationError, QueryValidationError, NotFoundError, sa.exc.NoSuchTableError)` collapses all four into one fixed `reason="not-available-to-you"` and one fixed message, never the real exception text or which validator raised — the latter two types were found missing from the original except clause during this item's own audit (`NoSuchTableError` in particular escaped as an uncaught 500, a fourth distinguishable outcome). `help/personal_denials.py`'s `_is_own_denial` excludes `operation="query_verdict"` events entirely, so a caller cannot read the real category back through their own denial history for the exact probe they just submitted. The compiled plan (SQL text + touched-table list) is omitted by default (`Policy.verdict_include_plan`, off by default) since it is itself data-dependent enough (which tables end up referenced) to be a discovery channel, and is never populated on the denied path regardless of the flag (the plan block sits strictly after the early-return denial). Unlike `explain` (a debugging tool a caller already needs identifier-level detail from, deliberately left as-is), this surface is audited (`operation="query_verdict"`) on every outcome including a quota/concurrency rejection, and is quota-metered (`enforce_query_quota`, the same per-principal budget `execute` consumes) **when the connection's policy configures a quota window** — off by default, same as every other read, so it is not unconditionally metered; a quota/concurrency failure propagates as a real error rather than being reported as a shape verdict. **Residuals, not closed by this control:** response *bodies* are identical across every denial cause, but response *timing* is not (schema validation does strictly more work than policy validation) — closing that would need a constant-time response floor, not implemented; `verdict` answers policy-and-schema shape only — it does not evaluate the approval gate (item 92) or the cost-estimation gate, so `allowed: true` does not guarantee unattended execution would proceed; and this is response-shape hygiene, not a privilege boundary — the same principal that can call `verdict` can call `mode="explain"`/`/query/explain` with the same credential and get the real validation message, since QueryGate has no scope today that distinguishes "may see a verdict" from "may see debug detail" (a verdict-only-caller confinement would need a new scope, an explicit product decision, not something this item adds) | `test_verdict_denied_by_policy_never_distinguishes_from_denied_by_schema`, `test_verdict_nonexistent_table_collapses_into_the_same_generic_denial`, `test_verdict_join_to_invisible_connection_collapses_into_the_same_generic_denial` (unit, three distinct root causes produce byte-identical `reason`/`message`), `test_query_verdict_denied_by_policy_and_by_schema_are_indistinguishable_end_to_end` (real SQLite reflection + real policy, three-way proof end-to-end including a wholly non-existent table), `test_query_verdict_plan_never_included_on_a_denial_even_when_opted_in_end_to_end`, `test_verdict_consumes_one_quota_unit`, `test_verdict_quota_exceeded_propagates_before_validation`, `test_verdict_quota_exceeded_is_still_audited`, `test_verdict_enforces_max_queue_depth`, `test_verdict_plan_included_only_when_policy_opts_in`/`test_query_verdict_plan_included_only_when_policy_opts_in_end_to_end`, `test_verdict_emits_a_redaction_safe_audit_event`, `test_verdict_many_never_leaks_a_validation_error_through_the_batch_wrapper`, `test_mcp_run_structured_queries_verdict_mode_never_executes_and_omits_plan`, `test_mcp_run_structured_queries_verdict_mode_denial_is_generic_through_the_real_transport`, `test_query_verdict_denials_are_excluded_entirely`/`test_own_denials_found_also_excludes_query_verdict_events` (`personal_denials.py`), `test_verdict_collapses_any_validation_failure_not_just_the_named_types` (fail-closed, not a type allow-list), `test_verdict_outer_handler_labels_not_found_correctly`, `test_verdict_plan_compile_failure_is_still_audited`, `test_verdict_enforces_max_queue_depth_per_principal`, `test_explain_enforces_max_queue_depth` |
| QG-36 | `GET /metrics` (Prometheus scrape endpoint, `metrics.py`) exposes low-level, per-connection operational counters — including `querygate_queries_rejected_total{reason=...}`, which already labels rejections `"policy"` vs `"schema"` for ordinary `execute`/`explain` traffic — without authentication, to any network caller who can reach it; separately, `verdict()` (TODO item 133) shared `execute()`'s per-principal quota budget while emitting no metrics of its own, so a caller could exhaust that budget through verdict calls alone and leave no verdict-shaped trace for an operator's metrics-based "why is this agent throttled" debugging (surfaced 2026-08-02 by the `security-invariant-reviewer` re-audit of item 133; TODO.md item 144) | `AppConfig.metrics_require_auth` defaults to **true**: `/metrics` requires a valid principal (the same `Authenticator`/scope machinery as every other admin surface) holding `admin:metrics:read` — its own least-privilege scope, deliberately separate from `admin:observability:read` (a different consumer: a Prometheus scraper vs. an admin-UI operator reading aggregated trends). Setting it `false` is an explicit, documented operator tradeoff for a deployment where the endpoint's network reachability is already restricted (e.g. a same-pod sidecar scrape path), not the default. Separately, `verdict()` now reports its own `querygate_verdicts_total{connection,outcome}` counter — `outcome` restricted to `allowed`/`denied` only, deliberately never a `reason` breakdown, so this fix cannot reopen the exact policy-vs-schema oracle QG-34 collapses — plus `querygate_verdict_duration_seconds`, and a verdict-driven quota exhaustion now increments the same `querygate_query_quota_rejections_total` counter `execute()` reports through | `test_metrics_endpoint_requires_auth_by_default`, `test_metrics_endpoint_requires_admin_metrics_read_scope`, `test_metrics_endpoint_reachable_unauthenticated_when_disabled`, `test_verdict_allowed_increments_verdicts_total`, `test_verdict_denied_increments_verdicts_total_without_a_reason_label`, `test_verdict_records_duration`, `test_verdict_quota_exceeded_increments_the_shared_quota_counter` |
| QG-37 | The regulated (fintech/healthcare) ICP's audit-retention question is "can you *produce* six months of untouchable records," not just "can you detect tampering in what you kept" (QG-32's residual) — without a native answer, a WORM archival failure could silently degrade retention with no operator-visible signal, or a redaction slip in the archived copy could become permanent (WORM's whole point makes an error unfixable) (TODO.md item 134) | `AuditSinkBackend.JSONL_CHAINED_S3_WORM` composes (never replaces) the local hash-chained ledger with an additional S3 Object Lock archive in COMPLIANCE mode — genuinely undeletable for the configured retention window, including by the AWS account root. `S3WormAuditSink.emit()` only ever buffers in-process (zero network I/O on the request path); a background `WormFlushMonitor` drains the buffer on a timer and PUTs one batched, Object-Lock-protected segment per flush — never one object per event, since Object Lock's retain-until timestamp is set per PUT and per-event objects would each expire at a slightly different moment as they age out. **Fails open by deliberate decision** (recorded in the PRODUCT_GUIDE Decision Log): a flush failure never blocks or fails the query that triggered the underlying event — the local chain sink already captured it for tamper-evidence — but the failed batch is re-queued for a retry (not silently dropped) and increments `querygate_audit_worm_flush_failures_total`, a metric an operator is expected to alert on; only a *sustained* outage past the buffer's bound (`AUDIT_WORM_MAX_BUFFERED_EVENTS`) drops the oldest events, visibly, via `querygate_audit_worm_buffer_dropped_total`. Redaction safety is structural, not a second implementation that could drift: the WORM sink never constructs its own event body — it serializes the exact same `PersistableEvent` the local sinks already write, proven byte-identical in tests. Registry-dispatched (`audit/sinks.py`'s `_SINK_FACTORIES`, mirroring `secrets/resolvers.py`'s `build_secret_resolver_registry`), not a scattered `if backend == ...` chain | `test_emit_only_buffers_never_touches_the_network`, `test_close_does_not_drain_the_buffer`, `test_flush_writes_one_object_lock_protected_segment`, `test_flushed_event_body_is_byte_identical_to_the_local_sink`, `test_flush_failure_fails_open_and_requeues_the_batch`, `test_flush_increments_the_flushes_and_archived_counters`, `test_over_capacity_drops_the_oldest`, `test_over_capacity_increments_the_dropped_metric`, `test_one_sub_sink_failing_does_not_suppress_the_other` (mutation-verified: a naive un-guarded `CompositeAuditSink.emit()` loop that stops at the first sub-sink's exception was confirmed to fail this test), `test_worm_audit_backend_wires_the_flush_monitor_end_to_end` (mutation-verified: a `WormFlushMonitor.start()` that's never called was confirmed to fail this test's `is_running` assertion) |
| QG-38 | A governed write (UPDATE/DELETE/INSERT) on a **MySQL** connection runs with no server-side execution ceiling, unlike Postgres (`statement_timeout`) and MSSQL (a query-timeout attribute paired with `LOCK_TIMEOUT`) — MySQL's `MAX_EXECUTION_TIME` session variable only bounds `SELECT` statements; an in-policy, in-cap write against a large or unindexed predicate can hold a connection and its InnoDB row locks for an unbounded duration (surfaced 2026-08-06 by the `security-invariant-reviewer` audit of item 19/128) | `MySQLSessionAdapter.apply_session_guardrails` (`connections/dialects.py`) still sets `innodb_lock_wait_timeout` (bounds lock *waits*, not total runtime) and `MAX_EXECUTION_TIME` (reads only) — this is a genuine MySQL capability gap, not a QueryGate omission, and is left as an **operator-facing deployment requirement** rather than emulated: run governed writes through a dedicated MySQL user/proxy configured with a server- or proxy-level write-timeout (e.g. ProxySQL `mysql-default_query_timeout`, or a connector-level statement timeout), since no session variable exists that MySQL itself honors for DML. Every other write guardrail (deny-by-default `WritePolicy`, `max_affected_rows`, batch caps, the approval gate) is dialect-agnostic and unaffected — this gap is specifically the *execution-time* ceiling, not authorization or blast-radius | None (documented residual, not implemented — no test asserts write-timeout coverage on any dialect; this residual exists specifically because that coverage cannot be provided server-side on MySQL) |
| QG-39 | `list_tables()` on an unseeded **MySQL** connection (no `known_tables` policy seed) leaked other databases' table names on the same server to any caller with no `table.allow`/`table.deny` restriction — unlike Postgres's/MSSQL's `INFORMATION_SCHEMA.TABLES`, which is already scoped to the connected database, MySQL's is server-wide and spans every database the connecting user has any privilege on, a cross-connection schema-shape disclosure of the kind QG-19/QG-24/QG-34 close elsewhere (found and fixed 2026-08-06 by the `security-invariant-reviewer` audit of item 19/128) | `SessionDialectAdapter.list_live_tables_extra_filter_sql()` (`connections/dialects.py`) adds a dialect-specific restriction to `schema/reflection.py`'s `list_live_tables()` query — a no-op default for Postgres/MSSQL (already scoped), and `TABLE_SCHEMA = DATABASE()` for MySQL, registry-dispatched rather than an inline `if dialect ==` branch. Table *names* only were ever exposed (never leaked column data, since `get_table_schema` reflects scoped to the default database) but names are exactly the schema-shape signal this codebase otherwise works to avoid leaking | `test_list_live_tables_extra_filter_only_restricts_mysql` (unit, registry dispatch), `test_list_tables_excludes_other_databases_on_the_same_server` (live: a second real MySQL database + table created on the same server, confirmed absent from `list_tables()` on an unseeded connection; mutation-verified — reverting the filter to a no-op was confirmed to make this test fail for the expected reason). The original item-19 system-schema exclusion test was itself found to be a false positive at the same time (it used a `known_tables`-seeded connection, which short-circuits `list_live_tables()` entirely, so it never actually exercised the code it claimed to guard) and was corrected to use an unseeded connection alongside this fix |
| QG-40 | A caller with `admin:observability:read` (granted for the routine anomaly/trend dashboard) automatically gains search access to the long-retention WORM compliance archive, or a search request against that archive triggers an unbounded/expensive S3 scan (no cap on time range, S3 objects fetched, individual object size, or wall-clock time — including inside a single day's `ListObjectsV2` pagination), or a caller pages an existing result set with a cursor that silently applies to a *different* filter set than the one that issued it, or a forged/corrupted archived line smuggles forbidden content nested inside `query_shape` rather than at the top level (TODO.md item 134 phase 2, `GET /api/v1/admin/observability/worm-search`) | Gated by its own scope, `admin:audit:worm-search` (`ADMIN_AUDIT_WORM_SEARCH_SCOPE`), deliberately not implied by `admin:observability:read` — the WORM archive is the durable, potentially multi-year compliance copy, a materially more sensitive surface than the in-process trend reads on the same router. Every request requires an explicit `start_time`/`end_time` (no "search everything" mode) capped at `AUDIT_WORM_SEARCH_MAX_WINDOW_DAYS` (default 730 days), and the actual S3 work within one request is separately bounded by `AUDIT_WORM_SEARCH_MAX_OBJECTS_SCANNED` and `AUDIT_WORM_SEARCH_REQUEST_TIMEOUT_SECONDS` — checked before every object fetch AND inside a day's own `ListObjectsV2` pagination (`MaxKeys` additionally caps each page to the remaining budget, so one oversized page can't slip past the check) — an out-of-bound request is rejected with a 422 before any S3 call is made; a bound hit mid-scan (including an individual object over `_MAX_OBJECT_BYTES`, checked via a lightweight size probe *before* the body is read into memory, or an object past the per-object line-count cap) degrades to a truncated, resumable page rather than continuing. The pagination cursor is a self-describing resumption token (not a bearer capability) carrying a fingerprint of the request's own filters (time range + `event_type`/`connection_id`/`principal_id`), so replaying a cursor against different filters is rejected (422); it cannot escape the issuing request's own bounds even if hand-edited, since `day` is re-validated against the current window, `key` is only matched against a freshly-listed prefix, and every event is still re-filtered by its own `occurred_at` regardless of what the cursor claims. `audit/worm_search.py` never constructs its own event body — every line read back is validated against the identical `extra="forbid"` event schemas the local sinks already write, so a returned event's top-level shape carries no more than a local audit-browser read already could; a segment tampered to carry a forbidden field (`sql`, `row_data`, `connection_string`) at the TOP level is rejected outright as malformed, and the one field `extra="forbid"` cannot reach — `query_shape`, a plain `Dict[str, Any]` — is separately screened for the same denylisted keys nested at any depth, closing the one gap a top-level-only check would have left open. **Partially fixed (TODO.md item 154), honestly scoped — closed only when `AUDIT_LEDGER_HMAC_KEY` is set, which is NOT the default:** WORM segments are now enveloped and per-segment hash-chained (`audit/worm_sink.py`'s `_build_segment_body`, the same `LedgerRecord` envelope the local sink uses), and this reader verifies each record's own hash before ever unwrapping it — a line that isn't envelope-shaped, or whose hash doesn't recompute, is counted as `malformed`/`unverified` and never returned. **With a keyed (HMAC) chain**, a principal holding `s3:PutObject` on the archive prefix (necessarily including QueryGate's own AWS role) can no longer plant a fabricated segment that passes this check, since forging a valid HMAC requires the key. **With the default unkeyed (SHA-256) chain**, the same principal CAN still forge a passing segment — SHA-256 is a public function computable by anyone, so this configuration detects accidental corruption and a naive/careless forgery, not a deliberate one, exactly the same limitation `audit/ledger.py`'s own docstring already states for the local ledger's unkeyed mode. Unlike the local ledger, the WORM chain has no compensating externally-anchored head hash to fall back on (each segment restarts at `GENESIS_PREV_HASH`), so the unkeyed WORM case is strictly weaker than the unkeyed local case. `src/querygate/api/app.py` now logs a startup warning when the WORM backend is configured without a key. **One residual remains, not closed by this control:** a genuine segment can still be silently withheld from a listing (an attacker would need to suppress an object from being listed, not forge one — left to the existing S3-listing/Object-Lock posture). **Partially closed by TODO.md item 172 (2026-08-10):** the reader used to verify each RECORD's own hash but never the CHAIN's linkage (`seq`/`prev_hash` continuity) within a segment; it now also verifies linkage — a record dropped from the middle of a segment, or one whose `prev_hash` doesn't continue from its predecessor, breaks the check (counted in `unverified`, plus a distinct `chain_breaks` count for the stronger signal), and nothing past the break is returned. **Made reachable by alerting — NOT continuously monitored — by TODO.md item 177 (2026-08-11):** both counts are also published as unlabelled Prometheus counters (`querygate_audit_worm_search_chain_breaks_total`, `querygate_audit_worm_search_unverified_total`), incremented on the served path AND when a scan fails against S3 partway through, so item 172's detection reaches the metrics pipeline instead of living only inside an ad-hoc search response. Alert on `chain_breaks_total`: unlike `unverified_total` it is not explained by a rotated `AUDIT_LEDGER_HMAC_KEY`. Deliberately unlabelled, matching item 126's `PERSONAL_DENIALS_RATE_LIMITED_TOTAL` — a `connection`/`principal` label would be caller-chosen cardinality and a weak activity oracle over the archive to anyone who can read `/metrics` (itself scope-gated by default since item 144, via `METRICS_REQUIRE_AUTH`). **Two limits stated explicitly, because an alert believed to be live but silent is worse than no alert:** (1) QueryGate never scans the archive on a schedule — `search_worm_archive` has exactly one production caller, this scope-gated route — so a counter only advances while a search actually runs, and a segment tampered inside a window nobody searches produces no signal; genuine coverage requires the operator to schedule a periodic search over a rolling window and to alert on the absence of searches too. (2) The counters count findings **per scan**, not distinct segments: a WORM object is immutable, so a real break is permanent and is re-counted by every later search reaching it (and by a client retrying a failed scan) — alert on the first non-zero increase and treat it as sticky until triaged, never on an absolute magnitude. A request cancelled by client disconnect or shutdown counts nothing at all (`asyncio.CancelledError` is not an `Exception`). **Still open, deliberately not closed by item 172 (interior omission only, not every omission):** a genuine, individually-valid, fully-intact segment can still be COPIED WHOLESALE to a second S3 key and returned twice — a linkage check can never catch this, since a full copy is itself a valid chain; closing it needs a further design decision (binding a segment to its own object key, e.g. deriving the genesis `prev_hash` from the key) recorded as its own follow-up before implementation. Records dropped from the TAIL of a segment (not the middle) are ALSO still undetectable by item 172: the surviving prefix is itself a perfectly valid chain (`seq 0..k-1`, every link intact) — unlike the local `audit/ledger.py` reader's `verify_chain`, which has an `expected_head` parameter specifically to catch records dropped from the end of a file, no per-segment head anchor exists outside the WORM object itself to compare against (WS-172-3, security-invariant-reviewer, 2026-08-10). **Also not covered:** rotating or first-setting the HMAC key makes every previously-archived segment fail verification on the next search — surfaced as a distinct `unverified` count (not `malformed`) in the response, disclosing the mismatch rather than returning a silently-empty, "nothing happened"-looking result, but historical-key support (trying more than one key) is not implemented | `test_the_observability_read_scope_alone_is_not_sufficient`, `test_requires_a_scope_at_all`, `test_the_dedicated_scope_is_sufficient_on_its_own` (`tests/integration/test_worm_search_api.py`); `test_missing_start_time_is_rejected`, `test_window_wider_than_the_cap_is_rejected`, `test_limit_above_the_cap_is_rejected`, `test_garbage_cursor_is_rejected`, `test_cursor_from_a_different_filter_set_is_rejected`, `test_max_objects_scanned_cap_truncates_mid_scan`, `test_request_timeout_cap_truncates_mid_scan`, `test_a_day_with_more_keys_than_the_object_budget_stops_listing_early`, `test_an_object_past_the_per_object_line_cap_truncates_instead_of_skipping`, `test_an_oversized_object_is_skipped_before_being_fully_read`, `test_a_default_limit_above_the_max_limit_is_a_configuration_error`, `test_paging_through_a_full_cursor_chain_yields_every_event_exactly_once` (`tests/unit/test_worm_search.py`); `test_a_predicate_referencing_a_sensitive_value_never_surfaces_that_value`, `test_a_forbidden_sql_field_on_an_otherwise_valid_event_is_never_returned`, `test_a_forbidden_field_nested_inside_query_shape_is_rejected_too`, `test_a_credential_shaped_field_smuggled_onto_an_event_is_rejected` (`tests/security/test_worm_search_redaction.py`); `test_a_bare_unenveloped_line_is_rejected_as_malformed`, `test_an_envelope_whose_hash_does_not_match_its_contents_is_rejected`, `test_a_genuine_segment_still_verifies_and_is_returned`, `test_a_keyed_chain_is_rejected_when_the_reader_has_no_key`, `test_a_keyed_chain_verifies_with_the_matching_key`, `test_enabled_backend_wires_the_ledger_key_through_to_verification` (`tests/unit/test_worm_search.py`, item 154); `test_records_dropped_from_the_end_of_a_segment_are_not_detected_yet`, `test_a_record_dropped_from_the_middle_of_a_segment_is_detected`, `test_a_segment_whose_first_record_is_not_the_genuine_genesis_is_detected`, `test_a_genuinely_intact_chain_across_multiple_segments_reports_no_breaks`, `test_resuming_mid_segment_does_not_false_positive_on_the_first_consumed_line`, `test_resuming_exactly_at_a_break_still_detects_it`, `test_metrics_and_note_still_disclose_key_rotation_separately`, `test_a_chain_break_below_the_line_cap_is_not_reported_as_a_capacity_truncation`, `test_the_note_discloses_how_many_lines_a_break_skipped`, `test_the_key_rotation_sentence_excludes_chain_break_lines` (`tests/unit/test_worm_search.py::TestChainLinkageVerification`, item 172, mutation-verified against the genesis check, the resumed-page seeding fix, the object-level "don't misreport as a capacity truncation" guard, and the skipped-lines counter); `test_a_chain_break_increments_both_integrity_counters`, `test_a_hash_mismatch_increments_unverified_but_not_chain_breaks`, `test_an_intact_archive_leaves_both_integrity_counters_untouched`, `test_a_chain_break_found_before_a_mid_scan_failure_is_still_counted`, `test_the_counters_carry_the_real_magnitude_not_just_a_boolean`, `test_malformed_lines_move_neither_integrity_counter`, `test_the_same_break_is_counted_once_per_scan_not_once_per_segment`, `test_both_integrity_counters_carry_no_labels`, `test_a_result_that_fails_to_build_counts_the_signals_once_not_twice`, `test_a_cancelled_scan_counts_nothing`, `test_the_worm_search_has_exactly_one_production_call_site` (`tests/unit/test_worm_search.py::TestMetrics`, item 177, mutation-verified against each counter's increment, the error-path call being dropped, boolean-ization of either counter, conflating `malformed` into `unverified`, and moving the counting call above the result-model construction — the last pinning the exactly-once invariant that justified deleting the drafted idempotence guard, and covering the previously-untested WS-7 "build first, count after" ordering for the request counter too); `test_the_resumed_page_seed_walk_does_not_scan_an_unbounded_blank_run`, `test_resuming_deep_inside_a_blank_run_past_the_walk_bound_fails_closed` (`tests/unit/test_worm_search.py`, item 172 follow-up WS-172-8 — the seed-walk fail-closed case, which is one of the three ways a `chain_breaks` finding arises); `test_a_cursor_pointing_past_the_end_of_an_object_does_not_crash` (`tests/unit/test_worm_search.py::TestPagination`, item 172 follow-up WS-172-7, mutation-verified — the resumed-page seeding fix's own backward walk indexed a caller-controlled cursor offset with no upper bound against the object's real length; clamped and pinned so a stale or hand-edited cursor degrades rather than raising `IndexError`); `test_flushed_segment_is_a_valid_per_segment_hash_chain`, `test_two_flushes_each_start_their_own_chain_at_genesis`, `test_flushed_event_body_is_byte_identical_to_the_local_chained_sink` (`tests/unit/test_audit_worm_sink.py`, item 154); `test_worm_audit_backend_wires_the_flush_monitor_end_to_end` (`tests/integration/test_rest_api.py`, item 154) |
| QG-41 | Snowflake (TODO.md item 19 phase 2) is a registered, hot-reloadable `ConnectionProfile` dialect with real compiler/session adapters, but `snowflake-sqlalchemy`'s driver has no async SQLAlchemy engine support — an operator could configure a `dialect: snowflake` connection expecting it to work, and `SnowflakeSessionAdapter`'s session guardrails/cancellation SQL (`ALTER SESSION SET LOCK_TIMEOUT`/`STATEMENT_TIMEOUT_IN_SECONDS`, `SYSTEM$CANCEL_ALL_QUERIES`) and its inherited no-op `list_live_tables_extra_filter_sql()` default are entirely unverified against a live account (found 2026-08-06 by the `security-invariant-reviewer` audit of item 19 phase 2) | `connections/engine.py`'s `init_engine` checks `SessionDialectAdapter.is_connectable()` (registered-interface dispatch, not an inline dialect comparison) and refuses to reach `create_async_engine` for any dialect that returns `False` — today, Snowflake and BigQuery (see QG-42 below) — raising a client-actionable `ConfigValidationError` (not a masked 500) that names the gap. So the connection is acceptable in config but a no-op in practice: no query, write, or schema reflection can ever reach a Snowflake server through this codebase yet, and every Snowflake-specific SQL claim in `compiler/dialect_adapters.py`/`connections/dialects.py` is sourced from Snowflake's public docs and checked only by compiling against a real, installed `snowflake.sqlalchemy` dialect object — never executed. **Residual, not closed by this control:** the guard keys on the connection's *declared* `dialect` field, not the connection string's actual URL scheme — a profile misconfigured with `dialect: postgresql` over a `snowflake://` URL would not be caught by this guard and would instead fail inside SQLAlchemy itself with a less clear error (tracked as a broader, dialect-agnostic gap in TODO.md item 158, since the same declared-vs-actual mismatch risk pre-exists for every dialect, not just Snowflake); `list_live_tables_extra_filter_sql`'s "no extra restriction needed" choice and every session-guardrail SQL statement remain unverified until TODO.md item 157 (live-server verification) closes it; and this guard is now also checked for the SECONDARY connection of a cross-connection join, not just the primary (TODO.md item 163, fixed 2026-08-07: `validation/schema_validation.py`'s `resolve_query_table_connections` calls the identical `get_session_adapter(...).is_connectable()` check on every joined connection, alongside its existing `join_group` check, and raises the same `ConfigValidationError`-shaped message — closing the gap where a not-connectable secondary used to sail past validation and, since reflection always runs through the PRIMARY connection's own engine (never the secondary's), instead fail later when the primary engine attempted to reflect the joined table under a schema qualifier borrowed from the secondary's connection string — a masked `NoSuchTableError` naming neither the real cause nor the secondary's dialect; never a data leak even before the fix, since `join_group` and the joined connection's own Policy still gated the join) | `test_snowflake_profile_is_rejected_before_create_async_engine`, `test_the_other_three_dialects_are_unaffected_by_the_snowflake_guard`, `test_create_async_engine_has_exactly_one_production_call_site` (`tests/unit/test_connections_engine.py`); `test_is_connectable_is_true_by_default_and_false_for_snowflake_and_bigquery` (`tests/unit/test_dialects.py`); `test_secondary_connection_not_connectable_rejected_at_validation_time` (`tests/unit/test_schema_validation.py`, item 163) |
| QG-42 | BigQuery (TODO.md item 19 phase 3) is a registered, hot-reloadable `ConnectionProfile` dialect with real compiler/session adapters, but `sqlalchemy_bigquery`'s driver has no async SQLAlchemy engine support — the same gap QG-41 documents for Snowflake — AND (genuinely new to BigQuery) its dialect resolves real Google credentials and builds a live `google.cloud.bigquery.Client` at engine-CONSTRUCTION time, not connection time, so an unguarded attempt would fail even earlier than Snowflake's does, with a confusing `google.auth.exceptions.DefaultCredentialsError` from a different library entirely rather than this codebase's own explained error. `BigQuerySessionAdapter`'s inherited no-op `list_live_tables_extra_filter_sql()` default is entirely unverified against a live project, the same residual class QG-41 documents for Snowflake (found 2026-08-07 during this item's own development, following the QG-41 precedent's own audit discipline) | Same control as QG-41, same registered `is_connectable()` seam, reused rather than reinvented: `connections/engine.py`'s `init_engine` refuses to reach `create_async_engine` for BigQuery, raising the identical client-actionable `ConfigValidationError`. Unlike Snowflake, BigQuery genuinely has no session-scoped SQL statement at all to send even if it were reachable — no SET/ALTER SESSION equivalent exists on BigQuery — so `BigQuerySessionAdapter.apply_session_guardrails` is a real no-op rather than an unverified rendered statement, and `capture_session_identifier`/`cancel_session` raise `NotImplementedError` rather than fabricate a plausible-looking session identifier for a session BigQuery never created (a genuine additional architecture gap beyond the missing async driver: BigQuery's real timeout/cancellation primitives are per-query-job, `QueryJobConfig.job_timeout_ms`/`jobs.cancel`, a materially different mechanism than every other adapter's session-level one). **Fixed (same control as QG-41, item 163):** this guard was originally only ever checked for a query's PRIMARY connection — a not-connectable dialect reached as the SECONDARY side of a cross-connection join sailed past it and, since reflection always runs through the PRIMARY connection's own engine, only failed later with a masked `NoSuchTableError` when that primary engine tried reflecting the joined table under a schema qualifier borrowed from the secondary's connection string (never a data leak, `join_group` and the joined connection's own Policy still gated the join). `validation/schema_validation.py`'s `resolve_query_table_connections` now checks `is_connectable()` on every joined connection too, raising the same `ConfigValidationError`-shaped message, so BigQuery as a secondary is rejected cleanly at validation time exactly like Snowflake — both dialects are exercised directly by the regression test, not just inferred from `is_connectable()`'s independent per-dialect coverage | `test_bigquery_profile_is_rejected_before_create_async_engine`, `test_the_other_three_dialects_are_unaffected_by_the_snowflake_guard`, `test_create_async_engine_has_exactly_one_production_call_site` (`tests/unit/test_connections_engine.py`); `test_is_connectable_is_true_by_default_and_false_for_snowflake_and_bigquery`, `test_bigquery_session_guardrails_are_a_genuine_noop`, `test_bigquery_capture_session_identifier_raises_rather_than_fabricates`, `test_bigquery_cancel_session_raises_rather_than_fabricates` (`tests/unit/test_dialects.py`); `test_secondary_connection_not_connectable_rejected_at_validation_time` (`tests/unit/test_schema_validation.py`, item 163) |
| QG-43 | The tail-first audit readers (`admin/anomaly.py`, `admin/config_trends.py`, and `help/personal_denials.py`, which reuses the same reader) gained a windowed early-exit (TODO.md item 141): once `max_consecutive_out_of_window` consecutive matching-type lines are all at-or-before the report's window start, the scan stops WITHOUT setting `truncated`, on the heuristic assumption that physical write order tracks `occurred_at` order. That assumption can be violated at scale by a merged/restored/multi-writer audit file — concatenated replica ledgers, a restored WORM segment spliced onto the live file, or an operator setting `num_of_workers > 1` in one pod/replica so multiple independent OS processes append to one `AUDIT_JSONL_PATH` with no cross-process lock — in which case the reader can confidently stop early on stale data and return a clean, undisclosed `truncated=False` while real recent events (a caller's own denials, a principal's anomaly signal) sit unread further back in the file. Found 2026-08-08 by three of the four `auditors` reviewers auditing item 141's own commit (surfaced during the item's own mandatory completion gate, not a later external report) | `audit/logger.py`'s `_persist` helper stamps `occurred_at` immediately before calling the sink's `emit()`, closing the *dominant* source of drift (arbitrary work — a `log.info` call, any future code — between event construction and the durable write) for the normal single-process case; this is real hardening, not just documentation. The residual gap above is disclosed rather than silently accepted: both readers' module docstrings, their `Protocol.load_query_events`/`load_change_events` docstrings, `AnomalyReport.truncated`'s and `ConfigCatalogChangeTrend.truncated`'s field comments, and `max_consecutive_out_of_window`'s own field comment all now name this exact failure mode and point here. `max_consecutive_out_of_window` has an operator-facing config knob per-surface (`anomaly_max_consecutive_out_of_window`/`change_trend_max_consecutive_out_of_window`/`personal_denials_max_consecutive_out_of_window`, item 141) so an operator who knows their deployment is merged/multi-writer can already raise or disable the tolerance without a code change. **Closed by item 171 (2026-08-10):** a `scan_ended_on_out_of_window_run: bool` field now distinguishes "genuinely complete" from "heuristically stopped early" on all three affected reports (`AnomalyReport`, `ConfigCatalogChangeTrend`, `RecentDenialsReport`), independent of `truncated` — the `AuditEventSource`/`ChangeEventSource` Protocols widened from a 3-tuple to a 4-tuple return, threaded through both `JsonlAuditEventSource`/`JsonlChangeEventSource` and each `build_*_report` function | `tests/unit/test_anomaly.py`'s and `tests/unit/test_config_trends.py`'s `test_jsonl_source_early_exit_*` tests now assert `scan_ended_on_out_of_window_run` directly (both that it's `True` on every heuristic-exit path and `False`/independent on a resource-bound stop), each new `test_report_discloses_the_heuristic_early_exit` pins the field reaching the report level, and `tests/unit/test_personal_denials.py`'s `test_report_respects_configured_max_consecutive_out_of_window` exercises the disclosed merged/multi-writer failure mode end-to-end (a caller's in-window denial sitting behind a run of out-of-window events from another principal) — mutation-verified: reverting the flag-setting line makes all of these fail for exactly that reason |

## 6. Error and data-disclosure policy

Policy, schema, not-found, and concurrency errors are actionable client errors
and may identify the caller-supplied table or column that was rejected.
Unexpected query/database exceptions are never returned verbatim through REST,
MCP, or batch results. Only explicitly typed client-validation failures are
actionable; a coincidental `ValueError` from a driver is still masked. Callers
otherwise receive `An unexpected error occurred.` and can provide the response
correlation id to an operator.

Operator stdout logs can contain stack traces, redacted SQL, intent text, and
database exception details. They are more sensitive than persisted audit JSONL
and must be access-controlled and retained accordingly. Policies should leave
`log_query_literals=false` unless literal SQL in operator logs is explicitly
acceptable.

## 7. Deployment requirements

The code controls above assume a correctly operated deployment. `deploy/`
(Docker Compose and Helm references, both verified against a real
deployment — see `deploy/README.md`) applies every requirement below by
default: non-root container user, no bundled database, Redis required once
more than one replica runs, and secrets kept out of version-controlled
config. Treat deviations from it as deliberate, reviewed decisions, not
defaults.

- Terminate TLS at a trusted proxy or at the service boundary; do not expose
  plaintext QueryGate traffic across an untrusted network.
- Set `ENVIRONMENT=production` and configure REST/MCP API keys or JWT. Never
  use the local anonymous mode on a shared host or network.
- Keep MCP DNS-rebinding protection enabled. Add the exact `Host` header used
  by the production proxy to `MCP_ALLOWED_HOSTS`; add browser origins only when
  they are intentionally supported.
- Give each database connection the least-privileged, read-only account
  QueryGate needs. Database grants are the final defense if application policy
  is misconfigured.
- Keep connection files and environment/secrets readable only by the service
  identity. Do not place production connection strings in source control.
- When using `${vault:...}` references, scope `VAULT_TOKEN`'s Vault policy to
  only the secret paths QueryGate needs (read-only), and rotate it through
  the deployment's normal secret-rotation process — QueryGate re-resolves
  every `${...}` reference on each config load/reload, so a rotated token or
  secret value takes effect on the next reload without a restart. Set
  `CREDENTIAL_LEASE_REFRESH_ENABLED=true` (item 135) to make that reload
  happen automatically, ahead of a leased reference's expiry, instead of
  only when an operator calls `/admin/reload-config` — closes the outage
  window a short-TTL dynamic credential would otherwise fall into between
  reloads. It only ever *triggers* the same reload/dispose path earlier; a
  reference whose resolver reports no lease (env, or Vault's current KV v2
  integration reading a static secret) is unaffected and stays reachable
  only through the operator-pull path.
- Use private, authenticated, TLS-protected Redis in multi-instance setups.
  Consider `CONCURRENCY_REDIS_FAIL_OPEN=false` when database protection is more
  important than availability during a Redis outage.
- Protect stdout logs and audit storage. When regulation requires immutable
  retention, either set `AUDIT_SINK_BACKEND=jsonl_chained_s3_worm` (native S3
  Object Lock archival, TODO.md item 134, QG-37) or ship events to your own
  retained/WORM storage or SIEM externally, and alert on
  `audit.sink.write_failed` and `audit.worm.flush_failed`.
- Restrict `/health` and admin routes at the network/proxy layer as
  appropriate for the environment. `/metrics` additionally requires the
  `admin:metrics:read` scope by default (`AppConfig.metrics_require_auth`,
  QG-36) — configure your Prometheus scraper with a bearer token carrying
  that scope, or set `metrics_require_auth: false` only if network-layer
  restriction alone is an explicit, deliberate tradeoff for your deployment.
- Grant `admin:config:write` only to identities that should be able to change
  what QueryGate connects to and enforces — treat it as equivalent in
  sensitivity to `admin:reload-config`. Grant `admin:config:read` more
  broadly if config-change visibility (not the ability to change it) is
  useful for an on-call/audit role; both are read-only into the same version
  history otherwise available only via the persisted audit trail.
- Grant `admin:connections:read` to on-call/monitoring identities that need to
  see which configured connection is failing (`GET /admin/connections`). It is
  read-only operational health — credential-free and never a connection string
  or raw driver error — and independent of the config scopes, so it can be
  granted to a role that should observe database reachability without being
  able to read or change configuration.
- Grant `admin:connections:test` more narrowly, only to operators who should be
  able to trigger a real, immediate connection attempt against a target
  database on demand (`POST /admin/connections/{id}/test`) — it is a distinct
  privilege from `admin:connections:read`, not implied by it.
- Back up `AppConfig.config_governance_dir` (default `var/config_versions/`)
  like any other durable state — it is QueryGate's own version history, not
  reconstructable from `connections.yaml`/`policy.yaml` alone once history
  has diverged from what's currently on disk.

## 8. Residual risks and explicit non-goals

- **Authorized inference:** QueryGate blocks use of denied columns, but a caller
  authorized for an aggregate can still infer facts from permitted counts and
  narrow filters. Two opt-in, off-by-default controls bound this and neither
  closes it: a minimum group size (item 88) and a cumulative disclosure budget
  over a rolling window (item 179). Differential privacy and query-set auditing
  are not implemented. See QG-29 and `docs/INFERENCE_RISKS.md` R3 — including
  the recorded gap that the budget's per-shape cap is currently evadable
  (item 186), so the per-table cap is the load-bearing one.
- **Pre-execution cost:** limits and timeouts are primarily reactive, but an
  optional Postgres `EXPLAIN`-based check (`Policy.max_estimated_rows`/
  `max_estimated_cost`, unset/disabled by default) can reject a likely full
  scan or join explosion before it executes (TODO item 26 phase 1). MSSQL has
  no equivalent yet (item 26 phase 2), and the check is fail-open: an EXPLAIN
  failure degrades to "not enforced for this query" rather than blocking it,
  so it is a complement to the reactive guardrails, not a replacement. The
  fail-open path is observable, not silent —
  `querygate_cost_estimation_unavailable_total{reason}` — and
  `Policy.cost_estimation_mode: observe` lets an operator measure what a
  threshold would reject against real traffic before switching a connection
  to `enforce`, since Postgres's planner-cost units aren't portable across
  schemas/hardware.
- **Large values in process memory:** the output byte cap prevents an oversized
  row from leaving QueryGate, but the database driver must first receive that
  row. Database-side statement limits and denial of large/blob columns remain
  important.
- **Malformed-input boundary is fuzzed, with one transport asymmetry left as a
  follow-up (TODO items 36 phase 2a / 84):** a broad corpus of malformed JSON
  (wrong types, missing/extra fields, invalid enums, invalid/empty JSON,
  non-finite numbers, and over-deep `where` trees) is rejected as a clean
  client error on both the REST and MCP surfaces without leaking any server
  internal or reaching the database (`test_malformed_input_fuzzing.py`). A
  non-finite number (`NaN`/`Infinity`) that used to 500 by making the REST
  validation-error response fail to encode is now a clean 422
  (`api/_errors.py`). The former REST/MCP asymmetry is closed (TODO item 86):
  a `where` nested past the JSON parser's recursion guard was a clean 400 over
  REST but a *handled* JSON-RPC internal-error (`-32603`, HTTP 500) over the
  mounted MCP transport, whose own `json.loads` raised `RecursionError`.
  `mcp/transport_guard.py`'s `MCPRequestGuardMiddleware` now wraps the MCP
  mount (outside auth) and rejects an oversized body (`413`) or an over-deep
  body (`400`) *before* the transport parses it — thresholds are configurable
  (`AppConfig.mcp_max_request_bytes` / `mcp_max_request_depth`) with generous
  defaults, so the MCP surface matches REST's "malformed input is a clean
  client error, never a 5xx" posture.
- **Configuration governance has version history and rollback, but no
  approval workflow yet:** the `/admin/config/*` API validates, versions,
  previews document-level changes, attributes, and audits every change, and a single `admin:config:write`
  caller can stage and apply a version in one session with no second-
  approver/four-eyes requirement or scheduled apply. `POST /admin/config/diff`
  (QG-20, TODO item 40 phase 1) adds a resolved-access semantic diff, but only
  at the connection baseline — per-principal resolution and blast-radius
  analysis are later phases. The preview is deliberately content-free; a
  write-only caller sees only submitted/inherited rather than an equality
  result, so write scope cannot be used as read scope. No admin UI (TODO item
  31).
- **Secrets lifecycle:** connection secrets resolve from either the
  environment or, optionally, HashiCorp Vault (token auth only — no
  AppRole/Kubernetes auth yet). `VAULT_TOKEN` itself is still a static,
  env-configured credential with no built-in rotation of its own; rotating
  it is an operator responsibility, same as any other credential. Other
  secret-manager backends (AWS/GCP Secrets Manager) remain unimplemented,
  though the `SecretResolver` interface is designed to add them without a
  breaking change. Item 135 added an optional, separate
  `LeasedSecretResolver` protocol and a `CredentialLeaseMonitor` background
  task that proactively triggers the existing reload before a leased
  reference's TTL expires — the trigger only, reusing item 13's
  reload/dispose machinery unchanged. The probing method
  (`lease_expiry`) deliberately returns no secret value, only an optional
  expiry — probing an expiry must never itself resolve/mint a credential,
  which matters for a future backend where reading is issuing (a Vault
  dynamic secrets engine). `VaultSecretResolver.lease_expiry` honestly
  reports Vault's own `lease_duration` field rather than synthesizing one;
  since the current Vault integration reads KV v2 (static secrets) only,
  that duration is typically `0` (no lease) in production today, so the
  monitor is real, tested infrastructure that activates automatically the
  day a registered resolver reads a path that genuinely carries a lease
  (e.g. a future dynamic-secrets-engine resolver) — it does not itself add
  dynamic-secrets Vault support, which was out of this item's scope. The
  monitor's source-file resolution is config-governance-aware (it prefers
  an active governed version's own files over the plain
  `connections.yaml`/`policy.yaml` paths, matching what
  `admin/service.py`'s `apply()` already does), each automatic trigger is
  offloaded off the event loop and records an `audit_config_change` event,
  and a hysteresis guard prevents a lease shorter than the refresh margin
  from retriggering on every poll — a post-ship security review caught the
  first two of these (the config-governance gap in particular, item
  QG135-01, could otherwise have silently reverted an approved policy
  change) before this item shipped.
- **Static-key identity:** every key in one configured API-key list shares one
  subject and scopes. Use JWT for per-human/per-agent identity and expiry.
- **Concurrency fail-open:** Redis-backed concurrency can intentionally fail
  open. This improves availability but temporarily removes the distributed cap.
- **Capacity waiting still has no cancellation or async contract (TODO item
  35 phase 3):** a caller may request `queue_mode=wait` up to the operator's
  own `concurrency_wait_seconds` ceiling (never longer — see QG-08), and
  `max_queue_depth`/`max_queue_depth_per_principal` now bound how many
  callers may wait at once (cross-replica when `concurrency_backend: redis`
  is selected — see QG-08), but there is still no MCP progress notification,
  REST `202`-plus-cancel contract, or way for a caller to actually cancel a
  query it's already waiting on/running. The in-process (non-Redis)
  `querygate_queue_depth` gauge remains single-process visibility only, like
  `querygate_concurrency_in_use`.
- **Audit durability:** the plain JSONL sink is not WORM storage, has no
  built-in retention or search, and sink failures do not fail an already-executed
  database query. The optional hash-chained ledger
  (`AUDIT_SINK_BACKEND=jsonl_chained`, QG-32) makes the trail tamper-evident but
  is not itself WORM: it assumes a single logical writer owns the chain head (use
  one replica, or a per-replica ledger file), detects tail truncation only
  against an externally anchored head hash, and — unkeyed — is tamper-evident
  only relative to that anchor. **`AUDIT_SINK_BACKEND=jsonl_chained_s3_worm`
  (TODO.md item 134) closes the "produce the records" half of this gap**:
  composed with (never replacing) the local chain, it additionally archives
  the same redaction-safe event bodies to S3 Object Lock in COMPLIANCE mode —
  genuinely undeletable for the configured retention window, including by the
  AWS account root. **Phase 2 (`GET /api/v1/admin/observability/worm-search`,
  `admin:audit:worm-search` scope) closes the "search the records" half**:
  a bounded, filtered, paginated search directly over that archive — a
  required and capped time range, `max_objects_scanned`/request-timeout caps
  on the actual S3 scan, and a resumable cursor when a bound truncates a
  page — so an over-wide or unbounded request is rejected rather than
  triggering a slow/expensive full-archive scan. It does not close the whole
  residual: WORM archival is still buffered and asynchronously flushed
  (fail-open — a sustained S3 outage can still lose the oldest buffered
  events past `AUDIT_WORM_MAX_BUFFERED_EVENTS`, visibly, via
  `querygate_audit_worm_buffer_dropped_total`, and a search request made
  during such an outage simply won't find events still sitting unflushed in
  the in-process buffer), and there is no browser UI over the search
  endpoint yet (REST only). It complements, not replaces, also shipping
  events to a SIEM.
- **Semantic memory phases 32A and 32B are complete; 32C is not:**
  provenance, row-free automatic refresh/diffs, selective staleness,
  deterministic policy-first retrieval, disabled/manual-only quarantined
  drafts, the fixed benchmark, a governed review/edit/approve/reject/
  publish/rollback workflow with audit events and durable version history,
  and connection-scoped export/import (backup/restore) and
  retention/deletion are present. There is still no hosted/local model
  adapter, embedding index, or usage-learning loop (32C). An unpublished draft remains a privileged,
  non-agent-visible record regardless of review status; only an explicitly
  published entry becomes agent-visible, and it is then filtered by policy
  like every other catalog entry. Schema freshness is `untracked` until a
  version-2 snapshot is persisted; stale state never changes access or
  blocks ordinary schema/query operations. Database comments remain only
  SHA-256 hashes in snapshots. Catalog version history is bounded to 2000
  entries and terminal-state records (rejected proposals; published-and-
  rolled-back proposals with their paired publish/rollback records) can be
  explicitly deleted (`catalog:delete`), but there is no automated
  retention schedule — pruning is an authorized, manual operator action.
- **Operator compromise:** a host/config administrator can change policy,
  secrets, logs, or the running process; QueryGate does not defend against a
  fully compromised control plane.
- **Supply chain and independent review:** signed artifacts/SBOM are TODO item
  30. No independent penetration test or formal certification has been
  performed.

## 9. Review triggers

Review and version this threat model whenever QueryGate adds a write path, a
new database dialect, stored procedures, a new authentication mechanism,
browser-facing UI, external secret/audit backend, query-cost engine, a new
admin/config mutation surface, or a change to the schema-catalog data model
(e.g. model-generated catalog content, item 32), product-guide retrieval, or
deployment-diagnostic context assembly. A release should also rerun
`make test-security` and the full default suite.
